{
  "name": "hr-payroll-packages",
  "version": "0.9.2",
  "private": true,
  "description": "Calculation engine, automation, journal, RTI and absence libraries",
  "scripts": {
    "test": "node test.js && node atest.js && node jtest.js && node rtest.js && node abstest.js && node itest.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
